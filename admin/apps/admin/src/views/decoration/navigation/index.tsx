// 装修列表
import { RouteMeta } from '@/router/routeMeta'
import {decorationList} from '@pkg/decoration'
export default decorationList

export const routeMeta: RouteMeta = {
  title: '装修列表',
  keepAlive: true,
  order: 9
}
