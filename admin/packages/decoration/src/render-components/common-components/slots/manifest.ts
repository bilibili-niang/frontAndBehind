import { defineSchema } from '@anteng/jsf/src/utils/schema'

export default {
  key: 'slots',
  name: '插槽',
  version: '0.1.0',
  thumbnail: '',
  schema: defineSchema({
    title: 'slots',
    type: 'object',
    properties: {}
  }),
  default: {}
}
